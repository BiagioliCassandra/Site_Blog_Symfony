{% extends 'base.html.twig' %}

{% block title %}Page Admin{% endblock %}

{% block body %}
<div class="m-4">
    <h1>{{ controller_name }}!</h1>
    <a class="btn btn-info my-4" href="{{ path('admin_user_add') }}">Ajouter un utilisateur</a>
    <a class="btn btn-info my-4" href="{{ path('admin_article_add') }}">Ajouter un article</a>
    <a class="btn btn-warning text-white my-4" href="{{ path('logout') }}">Déconnexion</a>
    <table class="table">
        <thead class="thead-dark">
          <tr>
            <th scope="col">id</th>
            <th scope="col">username</th>
            <th scope="col">roles</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <th scope="row">{{ user.id }}</th>
                <td>{{ user.username}}</td>
                {% for role in user.roles %}
                <td>{{ role }}</td>
                {% endfor %}
                {# <td> #}
                    {# <a href="{{ path('admin_user_edit') }}">Modifier un utilisateur</a> #}
                    {# <a href="{{ path('admin_user_delete') }}">Supprimer un utilisateur</a> #}
                {# </td> #}
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}

