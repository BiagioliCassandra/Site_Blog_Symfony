{% extends 'base.html.twig' %}

{% block title %}Ajouter un utilisateur!{% endblock %}

{% block body %}
<div class="w-50 m-4">
    <h1>{{ controller_name }}!</h1>
    <a class="btn btn-info my-4" href="{{ path('admin') }}">Retour</a>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-info">Créér</button>
    {{ form_end(form) }}
</div>
{% endblock %}